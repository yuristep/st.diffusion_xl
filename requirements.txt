# Основные зависимости для Stable Diffusion XL
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0

# Для работы с переменными окружения
python-dotenv>=1.0.0

# Дополнительные зависимости для обработки изображений
Pillow>=9.0.0
numpy>=1.21.0

# Для оптимизации (опционально)
# xformers>=0.0.20  # Ускоряет генерацию на GPU (требует CUDA)
